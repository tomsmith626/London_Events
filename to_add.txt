To add:
Add prices - if available
Add error catches for all data

To amend:
Correct approach to extracting data - current process likely to see repeats and not extract all events.
Clean formatting/style in CSS
Ensure only events with tickets are presented



Code dump:

            const priceDiv = document.createElement('div');
            nameDiv.className = 'price';
            try {
                let minPrice = data.priceRanges[0]['min']
            } catch (error) {
                let minPrice = "TBC"
            }
            priceDiv.innerHTML = `Minimum Price: £${minPrice}`;

            eventDisplayDiv.appendChild(priceDiv);

            
